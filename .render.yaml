services:
  - type: web
    name: fake-news-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python news.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
