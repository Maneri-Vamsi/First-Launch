# Remove --only-binary since it causes problems on Render

# Build dependencies (latest versions)
setuptools>=80.0.0
wheel>=0.45.0

# App dependencies compatible with Python 3.13
Flask==2.2.5
pandas==2.2.3
numpy==2.2.0
scikit-learn==1.4.2
nltk==3.8.1
gunicorn==21.2.0
